#include <string.h>

/**
 * *_strcat - string + string
 * @dest: string to return into
 * @src: string to add at end
 * Description: catenates two strings
 * Return: a string
 */

char *_strcat(char *dest, char *src)
{

	return (strcat(dest, src));

}
