#include "main.h"
#include <string.h>

/**
 * _strlen - return length of string
 * @s: pointer to string variable
 * Description: returns length of s
 * Return: length of s
 */

int _strlen(int *s)
{
	return (strlen(s));
}
